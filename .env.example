# Database location (relative path — will be created automatically)
DATABASE_URL=file:./db/custom.db

# Secret key for NextAuth session encryption
# IMPORTANT: Generate one with: openssl rand -base64 32
# Then paste the output below. Example: NEXTAUTH_SECRET=abc123xyz456randomstring789
NEXTAUTH_SECRET=
